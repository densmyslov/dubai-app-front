# Lambda Function URL (AWS) - Streaming responses
LAMBDA_FUNCTION_URL=https://your-lambda-url.lambda-url.region.on.aws/

# API Key for Lambda authentication
KEY=your-api-key-here

# WebSocket Lambda URL - Real-time backend updates
NEXT_PUBLIC_WS_URL=wss://your-websocket-url.execute-api.region.amazonaws.com/prod

# Optional: Override Claude model (uses server default from parameters.json if not set)
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Webhook Secret - Used for both chat webhook (/api/webhook) and chart webhook (/api/charts)
# This secret authenticates external POST requests to your webhook endpoints
WEBHOOK_SECRET=your-webhook-secret-here

# Optional: Separate secret for chart webhook only
# If not set, uses WEBHOOK_SECRET for both webhooks
# CHART_WEBHOOK_SECRET=your-chart-webhook-secret-here

# Note: API_AUTH_TOKEN is loaded from config/.secrets.json on the server
# Do not commit .env.local to git
